import React from 'react';

import styles from './Footer.module.css';

const Footer = (props) => {
    return <div className={styles.Footer} data-testid="Footer">Footer</div>
}

export default Footer;
