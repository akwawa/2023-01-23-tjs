import React from 'react';

import styles from './Header.module.css';

const Header = (props) => {
    return <div className={styles.Header} data-testid="Header">Header</div>
}

export default Header;
